//! Retrieval-augmented generation engine.
//!
//! Coordinates embedding, vector search against Qdrant, and prompt
//! assembly for the inference server. Stub for now.

#[derive(Debug, Default)]
pub struct RagEngine;

impl RagEngine {
    pub fn new() -> Self {
        RagEngine
    }
}
