//! Citation engine: maps retrieved chunks back to verified source
//! documents and builds the source references returned to clients.
//! Stub for now.

#[derive(Debug, Default)]
pub struct CitationEngine;

impl CitationEngine {
    pub fn new() -> Self {
        CitationEngine
    }
}
