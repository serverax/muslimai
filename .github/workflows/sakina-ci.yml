name: sakina-ci

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repo-guard:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sakina-only guard
        run: bash scripts/ensure-sakina-only.sh

  flutter-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.0"

      - name: Install Flutter dependencies
        run: |
          cd sakina-frontend
          flutter pub get

      - name: Flutter analyze
        run: |
          cd sakina-frontend
          flutter analyze

      - name: Flutter tests
        run: |
          cd sakina-frontend
          flutter test --reporter compact

  backend-ci:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: sakina
          POSTGRES_USER: sakina_user
          POSTGRES_PASSWORD: sakina_password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5434:5432
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Bootstrap Sakina test database
        env:
          DATABASE_URL: postgres://sakina_user:sakina_password@localhost:5434/sakina
        run: bash scripts/sakina/bootstrap-test-db.sh

      - name: Check formatting
        run: |
          cd sakina-backend
          cargo fmt -- --check

      - name: Run clippy
        run: |
          cd sakina-backend
          cargo clippy --locked --all-targets --all-features -- -D warnings

      - name: Run backend tests
        env:
          DATABASE_URL: postgres://sakina_user:sakina_password@localhost:5434/sakina
        run: |
          cd sakina-backend
          cargo test --locked --all-targets --all-features -- --test-threads=1

  security-scan:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sakina-only guard
        run: bash scripts/ensure-sakina-only.sh

      - name: Secret scan
        run: bash scripts/sakina/verify-no-secret-leak.sh --all-files

  android-build-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.0"

      - name: Install Flutter dependencies
        run: |
          cd sakina-frontend
          flutter pub get

      - name: Build Android APK check
        run: |
          cd sakina-frontend
          flutter build apk --debug --dart-define=SAKINA_API_BASE_URL=https://api.7jzi.com/v1

      - name: Upload Android artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sakina-android-check-artifacts
          path: |
            sakina-frontend/build/app/outputs/flutter-apk/*.apk
            sakina-frontend/build/app/outputs/bundle/**/*.aab
          if-no-files-found: ignore

  ios-build-check:
    if: github.event_name == 'workflow_dispatch'
    runs-on: macos-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.0"

      - name: Verify iOS target exists
        run: |
          if [[ ! -d sakina-frontend/ios ]]; then
            echo "sakina-frontend/ios is missing. Restore the iOS target before running this job."
            exit 1
          fi

      - name: Install Flutter dependencies
        run: |
          cd sakina-frontend
          flutter pub get

      - name: Build iOS without code signing
        run: |
          cd sakina-frontend
          flutter build ios --no-codesign --dart-define=SAKINA_API_BASE_URL=https://api.7jzi.com/v1

      - name: Upload iOS build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sakina-ios-check-artifacts
          path: |
            sakina-frontend/build/ios/**
          if-no-files-found: ignore
